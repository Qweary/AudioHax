[package]
name = "AudioHax"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
midir = "0.8"
rand = "0.8"
opencv = { version = "0.95.1", features = ["buildtime-bindgen"] }   # version may vary; pick a recent one
anyhow = "1.0"
