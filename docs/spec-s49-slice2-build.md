# S49 Slice 2 — The Buildable Per-File Spec: PER-ROLE RHYTHMIC IDENTITY + BETWEEN-SECTION RHYTHM ARC

**Author role:** Rust Architect (DESIGN ONLY — no source/test/asset modified by this document; `docs/` only).
**Date:** 2026-06-20.
**Status:** LOCKED for build. This is the single contract three implementers build against — the **Music Theory PRODUCER** (owns the `chord_engine.rs` realizer internals + the `assets/mappings.json` musical rows, single-writer on `mappings.json`), the **Rust Implementer** (owns the `composition.rs` planner side, where between-section work needs the planner not the realizer), and the **Test Engineer** (owns `tests/variety_scorecard_s45.rs`). The S46 work-order's Slice-2 row (`docs/design-s46-figure-ground.md` §5 row "2") + the PER-ROLE RHYTHM (sig 7) row (`design-s46-figure-ground.md` §3) are the settled inputs; this doc encodes them, it does not re-litigate them.

**Synthesizes** the S46 design cadence's Slice-2 row into a build: `design-s46-figure-ground.md` §5 row 2 (the four coupled items — per-role rhythm BIAS / phase-separate the bed onsets / melody articulation contrast / per-section density off region energy) + the §3 PER-ROLE RHYTHM gap-map row (signal 7: "Five distinct arms exist but all key off ONE shared `edge_activity`; between-role/section reads flat" → "Each role on a distinct onset grid; between-section density arc"). Mirrors `docs/spec-s47-slice1-build.md` and `docs/spec-s48-slice3-build.md` in shape and rigor.

**Grounded against the LIVE working tree at HEAD — every file:line below was re-read THIS session against the live file. The tree DRIFTED after S47/Slice-4 AND S48/Slice-3 landed; the S46 work-order line numbers are STALE and were NOT trusted. The confirmed live sites are below:**

`src/chord_engine.rs` —
- `realize_rhythm` `:1873-1899` (signature; private additive params `pad_voices :1885`, `ctx :1890`, **`counter_present :1898`** — the S48 precedent for a new private param). The frozen kernel only CALLS this free fn; `realize_step`'s public signature is unchanged.
- `edge_activity` computed `:1904-1915`: `base = (features.edge_density / EDGE_ACTIVITY_RANGE_MAX).clamp(0,1)` `:1905`, then the **S29 density nudge** `density_nudge = (ctx.section.density - 0.5) * DENSITY_ACTIVITY_GAIN` `:1913`, summed + clamped `:1914`. This `edge_activity` is the ONE shared busyness scalar EVERY role's band selection keys off — the flatness root the work-order names.
- `pre_cadence` `:1925-1928`; `curve_frac` (the continuous articulation curve) `:1961`; **`base_frac`** `:1962-1973` — the role match `:1962-1969` has ONLY a `HarmonicFill => curve_frac.max(ARTIC_WINDOW_LO)` arm (`:1967`) and `_ => curve_frac` (`:1968`); the per-character bias `.mul_add(BALLAD_ARTIC_BIAS, 0.0).clamp(ARTIC_WINDOW_LO, ARTIC_WINDOW_HI)` `:1972-1973`. **This is the live "Fill special-case" the work-order's `base_frac` item refers to.**
- The S48 inverse-comp `comp` factor + the secondary articulation detach `:1975-2004` (the `comp`-gated `base_frac` re-bind `:2000-2004`) — slice 2 must compose with these without re-touching them.
- The `sustained(offset, slot_ms, frac)` closure `:2013-2022`; the `is_cadence` early-return ring `:2034-2063`.
- Per-role dispatch `match role` `:2065`: **Bass** arm `:2066-2168` (the legacy sustained/pre-cadence body `:2124-2167`, the freeze default); **HarmonicFill** arm `:2147-2168` (`vec![sustained(0, step_ms, base_frac)]` `:2166`, rest-as-gesture `:2162-2164`); **Pad** arm `:2170-2270` (block bed offset-0 `:2234-2244`, the S20 figured branch `:2228-2231`, the S47-Slice-4 `recede_pad_onsets` cap `:2261-2269`); **CounterMelody** arm `:2272-2402` (the S47 governor `m_class` routing `:2379-2401`, MOVING at `step_ms/4` `:2386-2388`); **Melody** arm `:2404-end-of-match` (the 4-band ladder `:2439-2495` returning `(events, pushable)`, the S48 onset push `:2510+`).
- Helpers near `:1043-1287`: `ActivityClass { Sustained, Oblique, Subdividing }` (derives `Ord`) `:1050-1058`; `melody_activity_class(edge_activity, prom_shift, pre_cadence) -> ActivityClass` `:1073-1110` (the SHARED band-class helper, cutoffs at `:1102/:1104`); `prominence_weight(ctx, role)` `:1278-1287` (returns `PROMINENCE_NEUTRAL` on empty prominence); `recede_pad_onsets(events, cap, step_ms)` `:1233-1270` (the COUNT-preserving offset-displacement precedent — moves WHERE onsets sit, never HOW MANY).
- Consts: `MELODY_ARP_CUTOFF = 0.80` `:1061`, `MELODY_SYNC_CUTOFF = 0.55` `:1062`, `MELODY_DOTTED_CUTOFF = 0.25` `:1063`; `ACTIVITY_FLOOR_THRESHOLD = 0.50` `:1119`; `COMP_OFFSET_FRAC = 0.25` `:1151`, `COMP_ARTIC_DETACH = 0.10` `:1159`; `PROMINENCE_NEUTRAL = 0.5` `:985`, `PROMINENCE_RHY_SHIFT = 0.10` `:1015`; `ARTIC_WINDOW_LO = 0.55` `:1824`, `ARTIC_WINDOW_HI = 1.10` `:1825`; `EDGE_ACTIVITY_RANGE_MAX = 0.05` `:1806`; `DENSITY_ACTIVITY_GAIN = 0.5` `:1816`; `FILL_REST_ACTIVITY = 0.10` `:1856`; `PORTATO_FRAC = 0.70` `:1793`, `STACCATO_FRAC = 0.40` `:1792`, `LEGATO_FRAC = 0.95` `:1794`.

`src/composition.rs` (the PLANNER side — the between-section arc) —
- **`Section.density: f32`** `:1209-1210` (the per-section busyness knob; "slice 1 default 0.5 (no-op)"), the realizer-consumed field. `OrchestrationProfile.density: f32` `:493-496` (serde default `0.5` `:533-546`).
- **The per-section density is ALREADY SET from region energy** (S29/MX-4): `section_density = (DENSITY_NEUTRAL + DENSITY_ENERGY_SPAN * (energy_i - 0.5)).clamp(DENSITY_FLOOR, DENSITY_CEIL)` `:1602-1607`, assigned `density: section_density` `:1653`. The per-section `energy_i` is resolved in `resolve_offsets`-style code `:2085-2152` from the ranked region energies (`u.foreground_energy` / `u.background_energy` `:2100/:2105`), home/identity sections carrying `HOME_ENERGY_NEUTRAL` `:1364` so `f(0.5) == DENSITY_NEUTRAL == 0.5` exactly (the byte-stable hinge).
- Density consts: `HOME_ENERGY_NEUTRAL = 0.5` `:1364`, `DENSITY_NEUTRAL = 0.5` `:1365`, `DENSITY_ENERGY_SPAN = 0.30` `:1366`, `DENSITY_FLOOR = 0.35` `:1367`, `DENSITY_CEIL = 0.65` `:1368`.
- `ImageUnderstanding` region-energy fields ALREADY EXIST: `subject_energy :85`, `foreground_energy :86`, `background_energy :88` (NEW S18). **No new image field is required for slice 2.**

`tests/variety_scorecard_s45.rs` — `LayerVerdicts` carries `rhythm_distinct_frac` (the F5a per-role rhythm-distinctness fraction) + `bg_recession_violations` (the F5b hard regression count, `s46_recession_bound` driven to 0). F5a is REPORTED today; F5b is the HARD gate asserted at 0. (Line numbers per the S48 spec §1; the Test Engineer re-grounds against the live file.)

**THE FREEZE (binding):** `src/engine.rs` is BYTE-FROZEN at sha256 `e50c7db189a1585102a885fd1e975bf378b06a9ed56ce26993c6c767a2348261` (re-verified UNCHANGED this session via `sha256sum src/engine.rs` — see §0). **NOTHING in this slice touches `engine.rs`.** Every lever is freeze-reachable in `chord_engine.rs` (the realizer the kernel only *calls*) + `composition.rs` (the planner) + `assets/mappings.json` (data) + `tests/` (scorecard). Every per-role / per-step / per-section term is centered on the existing identity no-op (see §4).

---

## 0. PRE-BUILD FREEZE VERIFICATION

```text
$ sha256sum src/engine.rs
e50c7db189a1585102a885fd1e975bf378b06a9ed56ce26993c6c767a2348261  src/engine.rs   ✓ MATCH (verified this session)
```

The `ENGINE_SHA256` guard in `tests/variety_scorecard_s45.rs` asserts this same digest and is UNTOUCHED by this slice. If the digest does not match at build time, STOP — the freeze-neutrality witnesses (§4) are predicated on this exact byte image.

---

## 1. CURRENT-STATE GROUND TRUTH — what slice 2 inherits, pinned to CONFIRMED live lines

The S47/S48 hierarchy is LIVE: the melody is most-active (S47 governor + activity floor), on top (S47 seat guard), loudest-when-it-needs-to-be (S48 level finish), and the bed recedes in activity (S47-Slice-4 Pad cap) + holds figure when low-seated (S48 inverse-comp). **Slice 2 adds the THIRD figure cue — distinct per-role onset GRIDS — stacked on those two.** Two findings reshape the work-order:

| # | Work-order item | Live state (CONFIRMED) | What slice 2 must ACTUALLY do |
|---|---|---|---|
| **1** | Per-role rhythm BIAS (generalize `prom_shift`) | EVERY role's band selection keys off ONE shared `edge_activity` (`:1904-1915`). `prom_shift` (`melody_w` only, `:2419`) shifts ONLY the melody's OWN cutoffs against a fixed scalar; bed roles have NO band-shift term — Bass/Fill are hard-coded to one sustained tone, Pad to a held block. So between-role rhythm reads FLAT: all roles "see" the same busyness and resolve to fixed per-role bodies, never a per-role onset-GRID. | Add a **per-role rhythm BIAS** that shifts the *effective band cutoffs per role*: melody biased TOWARD subdivision (cutoffs lowered → reaches arpeggio/syncopation sooner → more active surface), bed roles biased AWAY (cutoffs raised / capped → steadier). Generalizes `prom_shift` from a melody-only nudge into a `role_rhythm_bias(role) -> f32` family. |
| **2** | Phase-separate the bed onsets off the melody's strong beat | Bed onsets sit at **offset 0** (HarmonicFill `:2166`, Pad block bed `:2242`) — the SAME downbeat the melody's DOTTED/SUSTAINED first onset sits on (pre-S48-comp). The melody's S48 comp pushes ITS onset off 0 only when low-seated; the bed never moves. So on a calm image the bed and melody fuse on the downbeat (the F5a anti-fusion concern). The `recede_pad_onsets` helper (`:1233-1270`) ALREADY displaces a Pad stab off the downbeat to a weak-beat fraction while preserving COUNT — the proven precedent. | Phase-separate the **HarmonicFill** (and, where the Pad still emits an offset-0 onset, the Pad) bed onset to a WEAK-beat fraction DISTINCT from the melody's downbeat AND from the counter's `step_ms/4` MOVING onset — a count-preserving offset displacement via the `recede_pad_onsets` precedent. |
| **3** | Melody articulation contrast (`base_frac` per-role bias) | `base_frac` (`:1962-1973`) has ONLY a HarmonicFill arm (`:1967`, floored to connected) + `_ => curve_frac` for everyone else. Melody, Bass, Counter all share the identical `curve_frac`. So the melody's articulation does not CONTRAST against the bed's — a third cue left on the table. The S48 inverse-comp detaches the melody `base_frac` ONLY when low-seated (`:2000-2004`). | Add a **per-role `base_frac` BIAS** beside the existing Fill arm: the melody biased toward MORE DETACHED / articulated (a crisper, more-foreground surface) vs the bed biased toward MORE CONNECTED (the existing Fill lean, generalized to the bed). Composes ADDITIVELY with the S48 comp detach (which still rides on top for the low-seat case). |
| **4** | Per-section density off image region energy | **ALREADY DELIVERED (S29/MX-4).** `Section.density` is set from region energy (`composition.rs:1602-1607`) and nudges `edge_activity` (`chord_engine.rs:1913`). This already makes a high-energy section busier and a calm one steadier — the between-section density arc EXISTS at the whole-texture level. | The residual slice-2 work is NOT "build per-section density" (built) but to (a) confirm the density arc is AUDIBLE as a between-section RHYTHM arc once per-role bias (item 1) is live (the bias makes the density swing read per-role, not just as a uniform activity shift), and (b) OPTIONALLY widen the swing magnitude (`DENSITY_ENERGY_SPAN`) if the taste gate finds the section-to-section contrast still reads flat. This is a PLANNER-side magnitude knob (Rust Implementer), not new mechanism. |

**The reshape (load-bearing).** Three of the four work-order items are realizer per-role differentiation (items 1-3, Music Theory PRODUCER in `chord_engine.rs`); the fourth is a magnitude tune on an ALREADY-BUILT planner mechanism (item 4, Rust Implementer in `composition.rs`). The per-role rhythm BIAS (item 1) is the load-bearing change — items 2 and 3 are the onset-phase and articulation faces of the same per-role-identity idea, and item 4's existing density arc only reads as a *rhythm* arc once item 1 lets the shared `edge_activity`/density resolve to per-role grids.

**Supporting seam facts (confirmed, load-bearing):**
- `realize_rhythm` is a free fn taking private additive params (`pad_voices`, `ctx`, `counter_present`) set by `realize_step`; `realize_step`'s PUBLIC signature is unchanged across S47/S48 and stays unchanged here. Any new input slice 2 needs (none is required — `role` and `ctx` are already in scope) follows that precedent.
- `prominence_weight(ctx, role)` returns `PROMINENCE_NEUTRAL(0.5)` on the empty/identity prominence Vec — the no-op anchor every weight-gated term centers on.
- `recede_pad_onsets` (`:1233-1270`) is the proven COUNT-preserving offset-displacement helper — the exact tool for item 2 (move WHERE a bed onset sits, never HOW MANY).

---

## 2. THE LEVER TABLE — each lever pinned, freeze-reachability confirmed

Tier legend: **[CE]** = `src/chord_engine.rs` realizer (kernel only calls it; identity path byte-neutral) · **[COMP]** = `src/composition.rs` planner · **[JSON]** = `assets/mappings.json` data. NO lever is in `engine.rs` (verified per-row).

| # | Lever | File:line region | Tier | Const / fn (neutral center) | Proposed range | Freeze-reachable? | Preserve-property it must not regress + why it can't |
|---|---|---|---|---|---|---|---|
| **L1** | `role_rhythm_bias(role) -> f32` — per-role band-cutoff shift, generalizing `prom_shift`. Melody +bias (cutoffs lowered → more subdivision), bed roles −bias (raised → steadier). | NEW helper near `melody_activity_class` `chord_engine.rs:1073`; CONSUMED in the band ladder `:2439-2495` (added to `prom_shift` term) + read into `melody_activity_class` calls (the governor `:2359`, the Pad cap `:2262`, the floor `:2430`) | **[CE]** | `role_rhythm_bias(role)`; **neutral center = 0.0 for EVERY role** (added to the existing `prom_shift`; identity weight 0.5 → `prom_shift == 0`; the per-role bias is a SEPARATE additive term centered at 0.0) | per-role magnitude TASTE-SIZED, conservative start `±0.05..0.10` (see §3) | YES — additive term centered at 0.0; the Melody bias only widens the melody's existing freeze-neutral cutoff shift; the BED biases are applied in arms unreachable under identity (no Counter/Pad/Fill instrument on the synthetic no-counter goldens). Outside engine.rs. | **F5b (stays 0)** + **F1 (melody-most-active)**: melody bias is POSITIVE (more onsets), bed bias is NON-POSITIVE (≤ melody) by construction → `bed_onsets ≤ melody_onsets` is preserved/strengthened, never inverted. The bias NEVER raises a bed role's onset count above the melody's. |
| **L2** | Bed onset PHASE-SEPARATION — displace the HarmonicFill (and any offset-0 Pad) bed onset to a weak-beat fraction off the melody downbeat. | HarmonicFill arm `chord_engine.rs:2166`; Pad block-bed branch `:2234-2244`; via the `recede_pad_onsets` precedent `:1233-1270` | **[CE]** | `BED_PHASE_FRAC` (the weak-beat fraction the bed onset sits at); **neutral center = 0.0 displacement under identity** (the bed roles are unreachable under the no-counter identity goldens; a phased-bed term is gated to fire only when a melody is present to phase against) | `BED_PHASE_FRAC` TASTE-SIZED, conservative start `step_ms * 0.5` (the half-beat, distinct from the counter's `step_ms/4` and the melody's downbeat 0); range `[0.375, 0.625] * step_ms` | YES — COUNT-preserving offset move (the `recede_pad_onsets` precedent); the HarmonicFill/Pad arms are not entered on the synthetic no-counter goldens (no Fill/Pad instrument there). Outside engine.rs. | **F5b/F1**: COUNT-preserving (moves WHERE, never HOW MANY) → onset counts unchanged → F5b/F1 untouched. **F5a (rhythm-distinctness)** can only IMPROVE (the bed leaves the melody's downbeat). **WATCH (deep-bed thinness):** the phase move must NOT drop the onset to below the bed FLOOR — if it would, fix from the `PAD_ONSET_FLOOR`/floor side, never by hollowing (see §6). |
| **L3** | Per-role `base_frac` BIAS — melody toward DETACHED (articulated foreground), bed toward CONNECTED (the generalized Fill lean). | The `base_frac` role match `chord_engine.rs:1962-1969` (ADD a Melody arm + a Bass arm beside the existing `HarmonicFill` arm `:1967`) | **[CE]** | `role_artic_bias(role)`; **neutral center = ×1.0 / +0.0** (the existing `_ => curve_frac` is the identity; the new arms nudge off it). Composes BEFORE the `.clamp(ARTIC_WINDOW_LO, ARTIC_WINDOW_HI)` `:1973` so it stays in the pleasant window, and BEFORE the S48 comp detach `:2000-2004` so the comp still rides on top. | TASTE-SIZED; melody detach start `−0.05` on `base_frac`, bed connect start the existing Fill `.max(ARTIC_WINDOW_LO)` generalized; range `[0.0, 0.12]` | YES — the bias is centered on the existing `curve_frac` (×1.0 = today) and re-clamped into the existing window; under identity the melody arm's `base_frac` is consumed only on the melody seat-79 goldens where the new arm must be a no-op at neutral (see §4 witness). Outside engine.rs. | **Cadence golden (240ms/114/84)**: the cadence ring early-returns `:2034-2063` BEFORE the per-role match — never reads `base_frac` → cadence untouched. **F1/F3**: articulation is DURATION, not onset count or pitch → F1/F3 untouched. |
| **L4** | Between-section density swing magnitude (OPTIONAL tune). | `DENSITY_ENERGY_SPAN = 0.30` `composition.rs:1366` (+ `DENSITY_FLOOR/CEIL` `:1367-1368`) | **[COMP]** | `DENSITY_ENERGY_SPAN`; **neutral center = the algebra is no-op at `energy_i == 0.5`** (`f(0.5) == DENSITY_NEUTRAL == 0.5` regardless of span) | TASTE-SIZED, current `0.30`; widen toward `[0.30, 0.45]` ONLY if the taste gate finds the section arc reads flat after L1 lands | YES — the home/identity section carries `HOME_ENERGY_NEUTRAL(0.5)` → `f(0.5) == 0.5` for ANY span value → identity density byte-stable → `density_nudge == 0` on the freeze path. Outside engine.rs. | **F5b/F1**: density nudges the SHARED `edge_activity`, which feeds the per-role bands — but L1's bed bias keeps bed ≤ melody at every density level; a busier section makes BOTH busier in rank order, never inverting. Bounded by `DENSITY_FLOOR/CEIL`. |

**Freeze-reachability summary:** L1/L2/L3 are `chord_engine.rs`-local realizer terms; L4 is a `composition.rs` planner const. **NONE is in `engine.rs`** — re-confirmed by direct read this session (every cited site is in `chord_engine.rs`/`composition.rs`). The freeze witnesses are in §4.

---

## 3. THE TASTE-SIZED MAGNITUDES — flagged OPEN for the taste gate (Affect §8 ∥ Aesthetics §9)

Every magnitude below ships with a CONSERVATIVE default so the producer/implementer builds immediately, AND is flagged TASTE-SIZED for the standing taste/affect + aesthetics gate (Specialist Marshaling Gate — slice 2 is generative/aesthetic work whose acceptance turns on "does each role read as its OWN rhythmic identity without the texture fragmenting?"). The SIGNS/DIRECTIONS are load-bearing and FIXED; the magnitudes want the operator's ear. **Affect §8 and Aesthetics §9 set the final numbers in the build cadence; do NOT lock them at build planning.**

| Taste-sized knob | Lever | CONSERVATIVE default (build start) | Range | Sign/direction (FIXED) | Source lens |
|---|---|---|---|---|---|
| **Per-role rhythm-bias magnitudes** (melody-toward-subdivision; bed-away) | L1 | melody **+0.05**, Fill/Pad **−0.05**, Bass **−0.08** (added to `prom_shift`) | melody `[+0.03, +0.10]`; bed `[−0.03, −0.10]` | melody POSITIVE (lower cutoffs → more active); bed NON-POSITIVE | Affect (cue rank 1: onset-rate) + Music Theory (role craft) |
| **Bed onset phase fraction** (`BED_PHASE_FRAC`) | L2 | `step_ms * 0.5` (the half-beat — distinct from the counter's `step_ms/4` and the downbeat 0) | `[0.375, 0.625] * step_ms` | POSITIVE (off the downbeat); must NOT equal `step_ms/4` (counter) or 0 (melody) | Affect (onset-asynchrony, cue rank 2) |
| **Melody articulation detach bias** | L3 | `−0.05` on melody `base_frac` (toward detached) | `[0.0, 0.12]` | NEGATIVE on melody `base_frac` (crisper foreground) | Aesthetics (articulation contrast, cue rank 4) |
| **Bed articulation connect bias** | L3 | the existing Fill `.max(ARTIC_WINDOW_LO)` generalized to the bed (toward connected) | `[0.0, +0.10]` toward `ARTIC_WINDOW_HI` | POSITIVE on bed `base_frac` (smoother bed) | Aesthetics |
| **Between-section density swing** (`DENSITY_ENERGY_SPAN`) | L4 | **0.30 — UNCHANGED (defer)** | optional → `[0.30, 0.45]` | wider = more section contrast | Affect + Aesthetics (does the section arc read?) |

> **The taste gate's binding question for slice 2.** Per-role distinctness has a FAILURE MODE the magnitudes must stay below: pushed too far, the texture FRAGMENTS (every role on a wildly different grid reads as noise, not counterpoint). The gate sizes the biases so each role is *recognizably its own line* while the ensemble still reads as ONE coherent texture. This is precisely a taste/affect judgment a correctness gate cannot render — F5a can confirm the grids DIFFER but not that they SOUND like counterpoint. The conservative defaults err toward coherence; the gate may widen them.

---

## 4. FREEZE-NEUTRALITY WITNESS — per lever, re-grounded on the confirmed lines

`engine.rs` is byte-frozen at sha256 `e50c7db189a1585102a885fd1e975bf378b06a9ed56ce26993c6c767a2348261`; the `ENGINE_SHA256` guard asserts it and is untouched. The **9/9 `engine_equivalence` goldens** are synthetic NO-COUNTER bars (melody note 79, bass 36, cadence velocities 114/84, cadence hold 240 ms).

| Lever | Why it is identity-byte-neutral (re-grounded on the confirmed live lines) |
|---|---|
| **L1 per-role rhythm bias** | `role_rhythm_bias` is a pure fn ADDED to the existing `prom_shift` term in the band ladder. For the **Melody** arm on the goldens: the goldens carry an EMPTY prominence Vec → `prominence_weight` returns 0.5 → `prom_shift == 0`; the per-role bias must therefore be **centered at 0.0 for the Melody role on the identity path**. The CLEAN witness: gate the melody rhythm bias on the SAME `counter_present` / foreground-weight pivot the S47 floor + S48 comp already use (`melody_w > ACTIVITY_FLOOR_THRESHOLD`), so at neutral 0.5 the bias is 0.0 → the melody-79 goldens select the EXACT same band (SUSTAINED, offset 0) → byte-identical. For the **bed roles** (Bass/Fill/Pad): their bias only matters inside their arms, and the synthetic goldens emit only melody-79 + bass-36 (no Fill/Pad/Counter) — the Bass arm's legacy body is the freeze default (`:2124-2167`) and a Bass rhythm bias must be a no-op there (the Bass golden is one sustained root at offset 0; the bias raises cutoffs → still SUSTAINED → unchanged). **WITNESS the Test Engineer confirms:** with the bias gated like the S47 floor (foreground-weight + counter-present), every golden's band selection is unchanged → 9/9 byte-green. |
| **L2 bed phase-separation** | The HarmonicFill arm (`:2147-2168`) and the Pad block-bed branch are NOT entered on the synthetic no-counter goldens (no Fill/Pad instrument there — the goldens are melody-79 + bass-36 bars). The phase displacement is a COUNT-preserving offset move (the `recede_pad_onsets` precedent, proven freeze-neutral in S47-Slice-4). On any non-identity render it moves WHERE the bed onset sits, never HOW MANY → onset counts byte-stable. **WITNESS:** confirm no golden bar emits a HarmonicFill/Pad event (they do not — same witness as S48's Fill-velocity-bias check, §4 there). |
| **L3 per-role `base_frac` bias** | The new Melody/Bass arms in the `base_frac` match (`:1962-1969`) must be CENTERED on the existing `curve_frac` (×1.0) at the identity path. The Melody bias is gated like L1 (foreground-weight) so at neutral 0.5 it is 0.0 → the melody-79 golden's `base_frac` is the unchanged `curve_frac` → byte-identical. The Bass bias must be a no-op on the bass-36 sustained golden (a small connect-bias that re-clamps into the existing window and does not change the sustained hold to a different rounded `hold_ms` — the Test Engineer confirms the rounded `hold_ms` is unchanged on the bass golden; if a non-zero bias rounds the bass hold differently, gate the Bass bias on `counter_present` too). **The cadence golden is structurally untouched** — the cadence ring early-returns (`:2034-2063`) before the per-role match, never reading `base_frac`. |
| **L4 density swing magnitude** | `f(energy_i) = DENSITY_NEUTRAL + DENSITY_ENERGY_SPAN * (energy_i − 0.5)`; the home/identity section carries `energy_i == HOME_ENERGY_NEUTRAL(0.5)` → `f(0.5) == DENSITY_NEUTRAL == 0.5` for ANY `DENSITY_ENERGY_SPAN` value → `Section.density == 0.5` → `density_nudge = (0.5−0.5)*GAIN == 0` (`chord_engine.rs:1913`) → `edge_activity` byte-identical on the freeze path. Changing the span CANNOT perturb identity. It changes already-routed multi-section renders only (the intended effect). |

**The one freeze item the lead MUST gate in the build order (§8):** the **L3 Bass `base_frac` bias witness** — confirm the bass-36 golden's rounded `hold_ms` is unchanged after the Bass arm lands (the Bass arm DOES run on the goldens, unlike Fill/Pad/Counter). The melody bias is gated freeze-neutral by the foreground-weight pivot; the bed-rhythm and bed-phase biases run only in arms the goldens never enter; the Bass `base_frac` bias is the only slice-2 term that touches a golden-reachable arm. The witness runs FIRST (Test Engineer), then the arm lands — exactly the S48 Fill-velocity-bias discipline.

---

## 5. WHAT F5a MUST OBSERVE — so the Test Engineer can promote/tighten it

The producer's changes are observable through the EXACT per-(step,role) `StampedEvent` streams `scorecard_for` already collects (per-role `offset_ms`, `hold_ms`, onset counts). **No new render path, no new image type, no audio, no OpenCV.**

**F5a (per-role rhythm-distinctness) is the slice-2 instrument.** It must be able to observe, per co-sounding step across the ensemble:

1. **Distinct onset COUNTS per role** (L1): the melody carries MORE onsets than the bed at a given `edge_activity`/density (the per-role bias resolves the SAME busyness to different bands). F5a's `rhythm_distinct_frac` should read the fraction of co-sounding steps where the per-role onset counts are NOT all equal — slice 2 drives this UP.
2. **Distinct onset OFFSETS per role** (L2): the bed's FIRST onset offset differs from the melody's downbeat (offset 0) AND from the counter's `step_ms/4`. F5a must read per-role first-onset `offset_ms` and confirm the bed sits at a DISTINCT phase. This is the same `offset_ms`-distinctness the S48 F4 metric already reads — F5a generalizes it across ALL bed roles, not just the inverse-comp melody.
3. **Distinct articulation (hold fractions) per role** (L3): the melody's `hold_ms / slot_ms` is SHORTER (more detached) than the bed's at the same step — F5a may optionally read per-role hold fractions to confirm articulation contrast (a secondary, taste-gated observable; the offset + count distinctness are the primary F5a quantities).

**Promotion discipline (the staged-bound M1.4 / S48-F4 pattern):** F5a is REPORTED today. The Test Engineer should:
- **Baseline** F5a's `rhythm_distinct_frac` on the pre-slice-2 tree (where it reads low — the flatness the work-order names).
- After L1+L2 land, **PROMOTE** F5a from reported toward a SIGN/threshold assertion on the counter-routed images: `rhythm_distinct_frac` exceeds its pre-slice-2 baseline by a margin (the exact threshold ear-tuned to the recession tiers — sign/direction asserted, magnitude reported).
- **F5b (the HARD regression gate) MUST stay at 0.** L1's bed bias is NON-POSITIVE (bed cutoffs raised → bed ≤ melody onsets); L2 is COUNT-preserving; L3 is duration-only. So `bed_onsets ≤ melody_onsets` per co-sounding step is preserved → F5b untouched. The Test Engineer baselines F5b on the pre-slice-2 tree (0) and confirms it stays 0.
- **F1 (melody-most-active)** reads onset counts — L1 strengthens it (melody biased toward MORE onsets), L2/L3 are count-neutral. No regression; F1 margin may improve.

**The producer adds NOTHING to the render path.** The Test Engineer adds the F5a promotion + re-confirms F5b == 0 + F1 margins hold.

---

## 6. PRODUCER vs IMPLEMENTER SPLIT

| Lever | Owner | Why |
|---|---|---|
| **L1 per-role rhythm bias** (`role_rhythm_bias` helper + band-ladder + `melody_activity_class` call-site wiring) | **Music Theory PRODUCER** (`chord_engine.rs`) | Pure realizer internals — the per-role band-cutoff biases are music-theory craft (which role is "active" vs "steady"), realized in the band ladder the PRODUCER owns. The 4-band → 3-class mapping and the per-role bias signs are theory calls. |
| **L2 bed onset phase-separation** (HarmonicFill/Pad onset displacement via `recede_pad_onsets`) | **Music Theory PRODUCER** (`chord_engine.rs`) | Realizer-local onset placement; reuses the PRODUCER-owned `recede_pad_onsets` precedent. Onset-phase is rhythm craft. |
| **L3 per-role `base_frac` bias** (Melody/Bass arms in the articulation match) | **Music Theory PRODUCER** (`chord_engine.rs`) | Articulation is the PRODUCER's realizer craft; composes with the S48 comp detach the PRODUCER already owns. |
| **L4 between-section density swing** (`DENSITY_ENERGY_SPAN` magnitude, IF the taste gate widens it) | **Rust Implementer** (`composition.rs`) | The PLANNER side. The between-section density mechanism is already built (S29/MX-4); the ONLY residual is a planner-const magnitude tune, which is structural/planner work (the Implementer owns `composition.rs`), not realizer music craft. The PRODUCER does not touch `composition.rs`. |
| (any `mappings.json` rows, if the biases are hoisted to data) | **Music Theory PRODUCER**, single-writer | The S42 single-writer discipline on `mappings.json` — the PRODUCER owns the musical rows; do not race the file. The slice-2 biases MAY be inlined CE consts (recommended for the ear-tuning loop) OR hoisted to a `mappings.json` per-role-rhythm catalogue (additive, serde-default-clean); either is freeze-neutral. |

**The split in one line:** the PRODUCER realizes the three per-role differentiation levers (L1/L2/L3) in `chord_engine.rs`, single-writer on any `mappings.json` rows; the Implementer owns the OPTIONAL planner-side density-swing magnitude (L4) in `composition.rs` — and L4 only fires if the taste gate asks for it after L1 lands. The Test Engineer promotes F5a + re-confirms F5b/F1.

---

## 7. CROSS-ARC / CROSS-SLICE INVARIANTS (state them so they are not broken)

- **The S47/S48 hierarchy is PRESERVED.** Melody stays most-active (L1 biases it TOWARD subdivision — strengthening, never weakening, F1), on-top (slice 2 touches rhythm/articulation, never pitch → the seat guard is untouched), and the S48 inverse-comp + level finish ride on top of L3 (L3 composes BEFORE the comp detach `:2000-2004`, so the comp still fires for the low-seat case). **S45 preserved:** the counter still moves (slice 2 does not touch the governor's MOVING/oblique routing; the bed phase-separation L2 is the FILL/PAD, distinct from the CounterMelody arm).
- **F5a anti-fusion (S42) + the S47 governor + S48 comp.** L2's bed phase fraction (`step_ms * 0.5` default) is DISTINCT from the counter's MOVING onset (`step_ms/4`, `:2386`), the Pad recession's surviving stab (`PAD_WEAK_BEAT_FRAC * step_ms`), and the melody's S48 comp push (`≈ step_ms/4` when low-seated). The producer CONFIRMS no offset collision on the 6-image set at the taste gate; the math says `step_ms/2 ≠ step_ms/4 ≠ 0` is safe. **If a collision is found, move `BED_PHASE_FRAC`, never the counter/melody onset.**
- **CLIMAX-BLOOM (S44 slice 2 / S46 tension #5).** L4's density swing makes high-energy sections busier — but L1's bed bias keeps the bed ≤ melody at EVERY density level, so the figure-ground gap widens with the bloom (the bed thickens UNDER a melody that thickens MORE). Slice 2 keeps the margin per-section-measurable (L1/L4 are per-step/per-section). DO NOT encode anything that pins a constant margin or makes the bias section-blind in a way that would block the later climax guard ("F1 margin at climax ≥ F1 margin at Statement").
- **The shared-cutoff coupling (S47 §8.2).** `melody_activity_class` MUST keep its cutoffs 1:1 with the live Melody-arm cutoffs. L1 adds a per-role bias TERM to the cutoff expression — the helper and the arm must read the SAME bias for the SAME role (recommend the producer threads `role_rhythm_bias(role)` into both, or extracts the biased cutoff to one source). Flagged so the governor's melody-class computation and the melody arm's band selection stay consistent.

---

## 8. RISKS / OPEN MICRO-DECISIONS + S47/S48 WATCH-ITEMS carried forward

**Open tensions / risks for the downstream lenses:**

1. **Texture fragmentation (the slice-2 failure mode).** Per-role distinctness pushed too far reads as noise, not counterpoint. The conservative defaults (§3) err toward coherence. **Taste gate sizes the biases; the FIXED constraint is that the ensemble must still read as ONE texture.** This is the central taste/affect judgment for slice 2.
2. **The 4-band → 3-class bias mapping (L1).** A per-role bias on the cutoffs interacts with the existing 4-band ladder (arpeggio/syncopated/dotted/sustained). The producer confirms the bias does not push a bed role into the ARPEGGIO band (a bed should never arpeggiate) — the NON-POSITIVE bed bias raises cutoffs (toward sustained), so this is structurally safe, but the producer confirms at the ear.
3. **L1 vs L4 interaction (the density arc reads as rhythm).** L4's existing density arc nudges the SHARED `edge_activity`; L1 makes that nudge resolve PER-ROLE. The producer/Implementer confirm the section arc is AUDIBLE as a rhythm arc after L1 (the work-order's "between-section reads flat" fix). If it still reads flat, L4 widens the swing — but ONLY after L1, never instead of it.
4. **Bed phase fraction collision (L2).** The default `step_ms/2` is collision-free against the counter (`step_ms/4`) and melody (0); the producer confirms on the 6-image set. The lead does not decide this — it is a producer taste-gate confirmation.

**S47/S48 WATCH-items folded into the design (ear-gated, carried forward):**

5. **Deep-bed thinness on busy-subject held passages.** If L2's phase-separation (or any slice-2 bed change) touches bed onset DENSITY, fix from the **bed FLOOR side** (`PAD_ONSET_FLOOR`-style), NEVER by hollowing the bed. L2 is COUNT-preserving by design (it moves WHERE, not HOW MANY), so it neither causes nor fixes thinness — but the producer confirms the phased bed onset is still PRESENT (not dropped below the floor). Carry as a taste-gate listen-item.
6. **The seat-lift leap (`MIN_FIGURE_GAP` 2→1).** OUT OF SCOPE for slice 2 unless a slice-2 lever forces it — none does (slice 2 touches rhythm/articulation, never the seat). Do NOT touch `MIN_FIGURE_GAP`.
7. **Single-writer on `mappings.json`.** IF any L1/L3 bias is hoisted to data, it goes through single-writer coordination (the S42 discipline) — Music Theory PRODUCER owns the rows; do not race the file.

---

*Design-only. No source, test, or asset modified by this document. `src/engine.rs` sha256 re-verified UNCHANGED this session: `e50c7db189a1585102a885fd1e975bf378b06a9ed56ce26993c6c767a2348261`.*
